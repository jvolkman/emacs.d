;; Mostly overrides for esk stuff

;; visible bell
(setq visible-bell nil)

;; allow selection deletion
(delete-selection-mode t)

;; turn on menu bar
(menu-bar-mode 1)

(setq find-file-wildcards t)

(provide 'jv-overrides)
